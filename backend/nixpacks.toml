[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = [
  "SECRET_KEY=build-phase-dummy DATABASE_URL=sqlite:///tmp/build.sqlite3 python manage.py collectstatic --noinput"
]
